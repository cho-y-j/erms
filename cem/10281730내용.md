# 2025-10-28 작업 내용 정리

## 📋 오늘 완료한 작업

### 1. 투입 관리 시스템 수정 완료 ✅

#### 문제점
- 투입 등록 시 EP 회사 선택이 있었음
- EP는 최종 승인 권자로만 역할하며, 투입 관리에는 관여하지 않음
- 반입 승인을 받으면 어느 BP 현장이든 투입 가능해야 함

#### 수정 사항
**파일: `client/src/pages/Deployments.tsx`**
- ✅ EP 필드 완전 제거 (epCompanyId 삭제)
- ✅ BP 회사 선택 드롭다운 추가
- ✅ 모든 BP 회사 목록 조회하도록 수정
- ✅ 백엔드는 이미 `epCompanyId: optional` 지원

**변경 내용:**
```typescript
// 상태에서 epCompanyId 제거
const [createFormData, setCreateFormData] = useState({
  entryRequestId: "",
  equipmentId: "",
  workerId: "",
  bpCompanyId: "",  // BP만 선택
  startDate: "",
  plannedEndDate: "",
});

// BP 회사 목록 조회
const { data: bpCompanies } = trpc.companies.list.useQuery({ companyType: 'bp' });
```

### 2. 투입 관리 기간 연장/운전자 교체 수정 완료 ✅

#### 문제점
- 기간 연장 실패: `nanoid is not defined` 에러
- 운전자 교체 실패: 동일한 에러
- 500 Internal Server Error 발생

#### 원인
- `server/db.ts`에 `nanoid` import가 없음
- `extendDeployment()`, `changeDeploymentWorker()` 함수에서 `nanoid()` 사용

#### 수정 사항
**파일: `server/db.ts`**
```typescript
// 2번째 줄에 추가
import { nanoid } from 'nanoid';
```

#### 테스트 결과
- ✅ 기간 연장 정상 작동
- ✅ 운전자 교체 정상 작동

### 3. 반입 요청 "-111" 문제 분석 ✅

#### 문제
- 투입 등록 시 "반입 요청 선택" 드롭다운에 "-111" 표시

#### 분석 결과
- **비정상 데이터**: 코드에서 생성 불가능한 값
- 정상 반입 요청 번호: `REQ-${Date.now()}` (예: REQ-1761611274693)
- 수동으로 입력된 테스트 데이터로 추정

#### 조치
- Supabase `entry_requests` 테이블에서 해당 레코드 삭제 권장
- 정상 데이터만 사용하면 문제 없음

---

## 🔍 작업 확인서 시스템 분석

### 요구사항 정리

#### 1. 일일 작업 확인서
**필수 필드:**
- 현장명
- 기사명 (자동: Worker 정보에서)
- 협력사명 (자동: Deployment에서)
- 차량번호 (자동: Equipment에서)
- 작업위치 (입력)
- 장비명 (자동: Equipment에서)
- 규격 (자동: Equipment에서)
- 작업내용 (텍스트 입력)
- 작업시간 (시작/종료 선택 → 자동 계산)
  - 일반근무 / OT / 철야 구분
- 작업 타입: 일대/월대/OT/철야 (선택)

**프로세스:**
1. Worker가 모바일에서 작성 제출
2. Owner에게 제출 알림
3. BP로 전달 → 서명 대기
4. BP 확인자 전자서명
5. 서명 완료 → PDF 자동 생성 및 저장

#### 2. 월별 작업 일지
**생성 방식:**
- 일일 작업확인서 자동 집계
- 매일 작성된 일일 확인서가 월별 일지에 누적

**서명 방식:**
- 일일 작업확인서 서명 시 자동으로 월별 일지에도 반영
- 또는 월말에 별도 서명

**필수 내용:**
- 총 작업일수
- 총 작업시간 (일반/OT/철야 구분)
- 일별 작업 내역
- BP 확인자 서명

#### 3. 계약 타입 및 단가
**투입 등록 시 추가 필요:**
- 계약 타입: 일대 / 월대
- 일대 단가
- 월대 단가
- OT 단가
- 철야 단가

→ 이후 월별 전산서 자동 생성에 활용

---

## 📊 데이터베이스 설계안

### 1. deployments 테이블 확장
```sql
ALTER TABLE deployments ADD COLUMN work_type VARCHAR(20);  -- 'daily' or 'monthly'
ALTER TABLE deployments ADD COLUMN daily_rate DECIMAL(10,2);
ALTER TABLE deployments ADD COLUMN monthly_rate DECIMAL(10,2);
ALTER TABLE deployments ADD COLUMN ot_rate DECIMAL(10,2);
ALTER TABLE deployments ADD COLUMN night_rate DECIMAL(10,2);
```

### 2. work_journal 테이블 확장
```sql
-- 기존 테이블에 컬럼 추가
ALTER TABLE work_journal ADD COLUMN deployment_id VARCHAR(64);  -- 투입 연결
ALTER TABLE work_journal ADD COLUMN bp_company_id VARCHAR(64);  -- 협력사
ALTER TABLE work_journal ADD COLUMN vehicle_number VARCHAR(50);  -- 차량번호
ALTER TABLE work_journal ADD COLUMN work_location VARCHAR(200);  -- 작업위치
ALTER TABLE work_journal ADD COLUMN equipment_name VARCHAR(100); -- 장비명
ALTER TABLE work_journal ADD COLUMN specification VARCHAR(100);  -- 규격
ALTER TABLE work_journal ADD COLUMN work_content TEXT;           -- 작업내용

-- 작업시간 상세
ALTER TABLE work_journal ADD COLUMN work_start_time VARCHAR(10);  -- 예: "08:00"
ALTER TABLE work_journal ADD COLUMN work_end_time VARCHAR(10);    -- 예: "18:00"
ALTER TABLE work_journal ADD COLUMN regular_hours DECIMAL(4,2);   -- 일반 근무
ALTER TABLE work_journal ADD COLUMN ot_hours DECIMAL(4,2);        -- OT
ALTER TABLE work_journal ADD COLUMN night_hours DECIMAL(4,2);     -- 철야

-- 서명
ALTER TABLE work_journal ADD COLUMN bp_signature_data TEXT;       -- 서명 이미지 base64
ALTER TABLE work_journal ADD COLUMN bp_signer_name VARCHAR(100);  -- 서명자 이름
ALTER TABLE work_journal ADD COLUMN signed_at TIMESTAMP;          -- 서명 일시

-- PDF
ALTER TABLE work_journal ADD COLUMN pdf_url VARCHAR(500);         -- PDF 파일 URL
```

### 3. monthly_work_reports 테이블 (신규 생성)
```sql
CREATE TABLE monthly_work_reports (
  id VARCHAR(64) PRIMARY KEY,
  deployment_id VARCHAR(64) NOT NULL,
  year_month VARCHAR(7) NOT NULL,          -- '2025-01'
  total_work_days INTEGER DEFAULT 0,
  total_regular_hours DECIMAL(8,2) DEFAULT 0,
  total_ot_hours DECIMAL(8,2) DEFAULT 0,
  total_night_hours DECIMAL(8,2) DEFAULT 0,
  total_amount DECIMAL(12,2),               -- 총 금액
  pdf_url VARCHAR(500),
  auto_signed BOOLEAN DEFAULT false,        -- 일일 서명 자동 반영 여부
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);
```

---

## 🎯 다음 작업 (내일 진행)

### Phase 1: 스키마 수정 및 마이그레이션

#### 1단계: schema.ts 수정
**파일: `drizzle/schema.ts`**

**작업 내용:**
```typescript
// 1. deployments 테이블 확장
export const deployments = pgTable("deployments", {
  // ... 기존 필드

  // 추가
  workType: varchar("work_type", { length: 20 }),  // 'daily' | 'monthly'
  dailyRate: decimal("daily_rate", { precision: 10, scale: 2 }),
  monthlyRate: decimal("monthly_rate", { precision: 10, scale: 2 }),
  otRate: decimal("ot_rate", { precision: 10, scale: 2 }),
  nightRate: decimal("night_rate", { precision: 10, scale: 2 }),
});

// 2. workJournal 테이블 확장
export const workJournal = pgTable("work_journal", {
  // ... 기존 필드

  // 추가
  deploymentId: varchar("deployment_id", { length: 64 }),
  bpCompanyId: varchar("bp_company_id", { length: 64 }),
  vehicleNumber: varchar("vehicle_number", { length: 50 }),
  workLocation: varchar("work_location", { length: 200 }),
  equipmentName: varchar("equipment_name", { length: 100 }),
  specification: varchar("specification", { length: 100 }),
  workContent: text("work_content"),

  workStartTime: varchar("work_start_time", { length: 10 }),
  workEndTime: varchar("work_end_time", { length: 10 }),
  regularHours: decimal("regular_hours", { precision: 4, scale: 2 }),
  otHours: decimal("ot_hours", { precision: 4, scale: 2 }),
  nightHours: decimal("night_hours", { precision: 4, scale: 2 }),

  bpSignatureData: text("bp_signature_data"),
  bpSignerName: varchar("bp_signer_name", { length: 100 }),
  signedAt: timestamp("signed_at"),

  pdfUrl: varchar("pdf_url", { length: 500 }),
});

// 3. monthlyWorkReports 테이블 신규 생성
export const monthlyWorkReports = pgTable("monthly_work_reports", {
  id: varchar("id", { length: 64 }).primaryKey(),
  deploymentId: varchar("deployment_id", { length: 64 }).notNull(),
  yearMonth: varchar("year_month", { length: 7 }).notNull(),
  totalWorkDays: integer("total_work_days").default(0),
  totalRegularHours: decimal("total_regular_hours", { precision: 8, scale: 2 }).default("0"),
  totalOtHours: decimal("total_ot_hours", { precision: 8, scale: 2 }).default("0"),
  totalNightHours: decimal("total_night_hours", { precision: 8, scale: 2 }).default("0"),
  totalAmount: decimal("total_amount", { precision: 12, scale: 2 }),
  pdfUrl: varchar("pdf_url", { length: 500 }),
  autoSigned: boolean("auto_signed").default(false),
  createdAt: timestamp("created_at").defaultNow(),
  updatedAt: timestamp("updated_at").defaultNow(),
});

export type MonthlyWorkReport = typeof monthlyWorkReports.$inferSelect;
export type InsertMonthlyWorkReport = typeof monthlyWorkReports.$inferInsert;
```

#### 2단계: 마이그레이션 실행
```bash
cd construction-equipment-management
pnpm db:push
```

### Phase 2: 백엔드 API 구현

#### 1. db.ts에 함수 추가
**파일: `server/db.ts`**

**작업 내용:**
- `getWorkJournalsByDeployment(deploymentId)` - 투입별 작업확인서 조회
- `createMonthlyReport(data)` - 월별 리포트 생성
- `getMonthlyReport(deploymentId, yearMonth)` - 월별 리포트 조회
- `updateWorkJournalWithSignature(id, signatureData)` - 서명 저장

#### 2. work-journal-router.ts 생성 또는 routers.ts 수정
**새로운 API 엔드포인트:**
- `workJournal.createWithDeployment` - 투입 기반 작업확인서 생성
- `workJournal.sign` - BP 서명
- `workJournal.generatePDF` - PDF 생성
- `monthlyReports.generate` - 월별 리포트 자동 생성
- `monthlyReports.getByDeployment` - 투입별 월별 리포트

### Phase 3: 프론트엔드 UI 구현

#### 1. 투입 등록 수정
**파일: `client/src/pages/Deployments.tsx`**
- 계약 타입 선택 (일대/월대)
- 단가 입력 필드 추가

#### 2. Worker 모바일 - 일일 작업확인서 작성
**파일: `client/src/pages/mobile/WorkLog.tsx` 수정**
- 투입 선택
- 작업내용 입력
- 작업시간 선택 (시작/종료)
- 제출 버튼

#### 3. BP - 작업확인서 서명
**파일: `client/src/pages/WorkJournal.tsx` 수정**
- 서명 대기 목록 표시
- Canvas 서명 패드
- 서명 저장 + PDF 생성

#### 4. 월별 리포트 페이지 (신규)
**파일: `client/src/pages/MonthlyReports.tsx`**
- 투입별 월별 집계
- PDF 다운로드
- 일별 상세 내역

### Phase 4: PDF 생성 기능

#### 라이브러리 설치
```bash
cd construction-equipment-management
pnpm add pdf-lib
```

#### PDF 템플릿 생성
- 일일 작업확인서 PDF 레이아웃
- 월별 일지 PDF 레이아웃
- 서명 이미지 삽입

---

## 📝 구현 우선순위

### 🔴 높음 (먼저 구현)
1. ✅ Schema 수정 (deployments, work_journal 확장)
2. ✅ 마이그레이션 실행
3. ✅ 투입 등록 시 계약 타입 & 단가 입력
4. ✅ Worker 모바일 작업확인서 작성 UI
5. ✅ BP 서명 기능

### 🟡 중간 (이후 구현)
6. PDF 자동 생성
7. 월별 리포트 자동 집계
8. 월별 PDF 생성

### 🟢 낮음 (나중에 구현)
9. 어드민 템플릿 시스템
10. 월별 전산서 자동 생성

---

## 🛠️ 기술 스택

### PDF 생성
- **pdf-lib**: PDF 생성 및 수정
- **Canvas API**: 서명 입력
- **Supabase Storage**: PDF 파일 저장

### 서명
- **HTML Canvas**: 터치/마우스 입력
- **Base64 인코딩**: 서명 이미지 저장

---

## ⚠️ 주의사항

### 1. 데이터 무결성
- Deployment와 WorkJournal 1:N 관계 유지
- 삭제 시 연결된 작업확인서 처리 방법 결정

### 2. 서명 유효성
- 서명 후 수정 불가 처리
- 서명자 인증 확인

### 3. PDF 저장
- Supabase Storage 용량 고려
- 파일명 규칙 정의 (예: `work-journal-{id}-{date}.pdf`)

---

## 📚 참고 자료

### 기존 구현 코드
- `drizzle/schema.ts:267-283` - work_journal 스키마
- `server/routers.ts:878-960` - work_journal API
- `client/src/pages/WorkJournal.tsx` - 작업확인서 페이지
- `client/src/pages/mobile/WorkLog.tsx` - 모바일 작업일지

### 관련 문서
- `건설장비 및 인력관리 시스템(ERMS) - 프로젝트 인수인계 문서.md`
- `CLAUDE.md` - 프로젝트 구조 설명

---

## 🎯 내일 시작할 작업

**1순위: Schema 수정**
1. `drizzle/schema.ts` 열기
2. `deployments` 테이블에 계약 타입/단가 필드 추가
3. `workJournal` 테이블에 상세 필드 추가
4. `monthlyWorkReports` 테이블 신규 생성
5. `pnpm db:push` 실행하여 마이그레이션

**2순위: 투입 등록 UI 수정**
1. `Deployments.tsx` 수정
2. 계약 타입 선택 추가 (일대/월대)
3. 단가 입력 필드 추가
4. API 연결

**3순위: Worker 모바일 UI**
1. `mobile/WorkLog.tsx` 수정
2. 투입 선택 기능
3. 작업 내용 입력 폼
4. 제출 기능

---

## 💭 논의 필요 사항

### 1. 템플릿 시스템
- 어드민에서 템플릿 만드는 기능을 Phase 2로 미룰지?
- 아니면 고정 템플릿으로 먼저 구현 후 확장할지?

### 2. 월별 서명 방식
- 일일 서명 자동 반영 방식?
- 월말 별도 서명 방식?
- 둘 다 지원?

### 3. 작업시간 계산
- 시작/종료 시간에서 자동 계산
- 점심시간 자동 제외? (예: 12:00-13:00)
- 수동 조정 가능 범위?

---

## ✅ 체크리스트 (내일 사용)

- [ ] schema.ts 수정 완료
- [ ] 마이그레이션 실행 완료
- [ ] deployments 테이블 확장 확인
- [ ] work_journal 테이블 확장 확인
- [ ] monthly_work_reports 테이블 생성 확인
- [ ] Supabase 대시보드에서 테이블 구조 확인
- [ ] 투입 등록 UI 수정
- [ ] Worker 작업확인서 작성 UI 시작

---

**다음 파일 이름: `10291730내용.md` (내일 작업 후)**
